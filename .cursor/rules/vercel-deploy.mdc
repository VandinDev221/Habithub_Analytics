---
description: Estrutura do projeto e deploy na Vercel
alwaysApply: true
---

# Habithub Analytics — Estrutura e deploy

## Estrutura do repositório

- **Frontend (Next.js):** pasta **`frontend/`** — `app/`, `components/`, `lib/`, `public/`, `package.json`, `next.config.js`, etc.
- **Backend (Express):** pasta `backend/`. API REST, PostgreSQL, rotas em `backend/src/`.
- Na raiz: `README.md`, `SISTEMA.md`, `.env.example`, `docker-compose.yml`.

## Deploy na Vercel

Configurar **Root Directory** = **`frontend`** em Settings → Build and Deployment. Ver `VERCEL_DEPLOY.md`.

## Favicon

`frontend/public/icon.svg` e `frontend/app/icon.svg`. Redirecionamentos em `frontend/next.config.js` para `/favicon.ico` e `/favicon.png` → `/icon.svg`.
