# Railway: build e start do backend (Node/Express)
[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && npm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
